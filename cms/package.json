{
  "name": "MoeyActivityKioskCMS",
  "description": "CMS made with KeystoneJS",
  "private": true,
  "version": "5.0.0",
  "author": "Seiya Kobayashi",
  "engines": {
    "node": ">=8.4.0 <13"
  },
  "config": {
    "PORT": "8011"
  },
  "scripts": {
    "dev:setup-all": "cross-env NODE_ENV=development DISABLE_LOGGING=true SETUP=all PORT=$npm_package_config_PORT keystone dev",
    "dev:setup-general": "cross-env NODE_ENV=development DISABLE_LOGGING=true SETUP=general PORT=$npm_package_config_PORT keystone dev",
    "dev:setup-activities": "cross-env NODE_ENV=development DISABLE_LOGGING=true SETUP=activities PORT=$npm_package_config_PORT keystone dev",
    "dev:setup-colors": "cross-env NODE_ENV=development DISABLE_LOGGING=true SETUP=colors PORT=$npm_package_config_PORT keystone dev",
    "dev:setup-users": "cross-env NODE_ENV=development DISABLE_LOGGING=true SETUP=users PORT=$npm_package_config_PORT keystone dev",
    "dev": "cross-env NODE_ENV=development DISABLE_LOGGING=true PORT=$npm_package_config_PORT keystone dev",
    "build": "cross-env NODE_ENV=production PORT=$npm_package_config_PORT keystone build",
    "start": "cross-env NODE_ENV=production PORT=$npm_package_config_PORT keystone start"
  },
  "dependencies": {
    "@keystonejs/adapter-mongoose": "^5.0.0",
    "@keystonejs/app-admin-ui": "^5.0.0",
    "@keystonejs/app-graphql": "^5.0.0",
    "@keystonejs/app-static": "^5.0.0",
    "@keystonejs/auth-password": "^5.0.0",
    "@keystonejs/fields": "^5.0.0",
    "@keystonejs/fields-wysiwyg-tinymce": "^5.0.1",
    "@keystonejs/file-adapters": "^5.1.0",
    "@keystonejs/keystone": "^5.0.0",
    "@keystonejs/list-plugins": "^5.0.0",
    "cross-env": "^5.2.0",
    "dotenv": "^8.2.0"
  }
}

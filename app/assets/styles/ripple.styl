#ripple
  border-radius: 50%
  transform: scale(0)
  position: absolute
  opacity: 1
  z-index: 99999
  &.ripple-effect
    animation: rippleDrop 0.3s linear
    @keyframes rippleDrop
      100%
        transform: scale(2)
        opacity: 0
